//! RustCash terminal user interface.
//!
//! Built with ratatui. Provides an account tree, transaction register, and
//! reconciliation workflow for power users who live in the terminal.

fn main() -> anyhow::Result<()> {
    println!("rustcash-tui — TUI interface (Phase 3)");
    Ok(())
}
