//! RustCash desktop GUI stub.
//!
//! The real implementation will use Tauri to wrap a web frontend over the local
//! API server. This stub is a placeholder until Phase 4.

fn main() -> anyhow::Result<()> {
    println!("rustcash-gui — Tauri desktop app (Phase 4)");
    Ok(())
}
